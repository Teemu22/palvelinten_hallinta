# H5 - Gitar Hero

## Tiivistelmät

### What is Git?
- Git on versionhallintajärjestelmä, jonka avulla voidaan seurata tiedostoihin tehtyjä muutoksia ajan kuluessa
- Se mahdollistaa palaamisen aiempiin versioihin ja muutosten vertailun
- Git tallentaa projektista tilannekuvia (snapshot), ei pelkästään muutoksia
- Jokaisella käyttäjällä on oma paikallinen kopio projektista
- Muutokset voidaan jakaa muiden kanssa esimerkiksi GitHubin kautta

**Oma huomio: Git ei ole pelkkä tiedostojen siirtotyökalu, vaan sen pääidea on muutosten hallinta ja yhteistyön helpottaminen.**

### Gitin peruskomennot
- ```git add``` lisää muutetut tai uudet tiedostot versionhallintaan (staging area)
- ```git commit``` tallentaa muutokset ja luo uuden version projektista
- ```git push``` lähettää paikalliset muutokset etävarastoon (esim. GitHubiin)
- ```git pull``` hakee etävaraston muutokset paikalliseen projektiin
- ```git add``` valitsee, mitä muutoksia halutaan mukaan seuraavaan commitiin
- ```git commit``` vaatii viestin, jossa kuvataan mitä muutoksia on tehty
- ```git push``` tekee muutokset näkyväksi muille käyttäjille
- ```git pull``` varmistaa, että oma versio on ajan tasalla ennen uusien muutosten tekemistä

**Oma huomio: Näitä komentoja käytetään yleensä yhdessä järjestyksessä add → commit → push, ja pull ennen sitä, jotta vältetään ristiriidat.**

### a.) Online

Tässä tehtävässä luotiin uusi GitHub-repository ja yhdistettiin se paikalliseen koneeseen Gitin avulla. Tavoitteena oli harjoitella versionhallinnan perustoimintoja, kuten repositorion luontia, kloonausta, tiedostojen lisäämistä sekä muutosten viemistä GitHubiin.

Aluksi loin GitHubiin uuden repositoryn nimeltä "sunshine". Repositoryyn lisättiin heti luontivaiheessa README-tiedosto sekä GNU GPLv3 -lisenssi.

<img width="1250" height="660" alt="image" src="https://github.com/user-attachments/assets/f80e7bdf-866a-4213-b2d5-0d37a12ba026" /> **Kuvassa luotu GitHub-repository nimeltä "sunshine", johon on lisätty README-tiedosto ja GNU GPLv3 -lisenssi.**

Repositoryn luomisen jälkeen se kloonattiin paikalliselle koneelle git clone -komennolla. Tämä mahdollistaa sen, että projektia voidaan muokata omalla koneella ja muutokset voidaan myöhemmin lähettää takaisin GitHubiin.

<img width="1279" height="324" alt="image" src="https://github.com/user-attachments/assets/de58a7bf-9604-4b84-ac0b-45cf1d15175f" /> **Kuvassa GitHub-repository kloonataan paikalliselle koneelle git clone -komennolla ja siirrytään projektikansioon.**

Kloonauksen jälkeen siirryin projektikansioon ja tarkistin sen sisällön. Kansiossa näkyivät GitHubissa luodut README.md- ja LICENSE-tiedostot, mikä vahvisti että kloonaus onnistui oikein.

<img width="1277" height="157" alt="image" src="https://github.com/user-attachments/assets/fb2e1384-9667-43d7-9107-36afcea64f69" /> **Kuvassa projektikansion sisältö, jossa näkyvät README.md ja LICENSE -tiedostot.**

Seuraavaksi loin uuden tiedoston ja lisäsin sen versionhallintaan git add -komennolla. Tämän jälkeen tallensin muutokset git commit -komennolla ja lähetin ne GitHubiin git push -komennolla.

Git commit -komennolla tallennetaan muutokset paikalliseen versionhallintaan, ja git push siirtää nämä muutokset etävarastoon (GitHubiin).

<img width="1279" height="314" alt="image" src="https://github.com/user-attachments/assets/bed1d638-9d56-4552-953a-812077561e97" /> **Kuvassa lisätään uusi tiedosto versionhallintaan git add ja git commit -komennoilla sekä lähetetään muutokset GitHubiin git push -komennolla. Kuvasta myös näkee, että olen tehnyt ne jo aiemmin.**

<img width="957" height="383" alt="image" src="https://github.com/user-attachments/assets/a84036a3-59e1-4d1e-97b4-af4dd6ecd900" /> **Kuvassa GitHub-repository, johon uusi tiedosto on ilmestynyt git push -komennon jälkeen.**

Lopuksi tarkistin GitHubista, että uusi tiedosto oli siirtynyt sinne onnistuneesti. Tämä vahvisti, että versionhallinnan perusprosessi (add → commit → push) toimii oikein.

Tämä vaihe vastaa tehtävänannon vaatimusta, jossa luodaan repository, tehdään siihen muutoksia paikallisesti ja varmistetaan, että muutokset näkyvät GitHubissa.

### b.) Dolly

Tässä tehtävässä kloonattuun GitHub-repositoryyn tehtiin muutoksia paikallisella koneella ja muutokset vietiin takaisin GitHubiin. Tavoitteena oli harjoitella versionhallinnan perusprosessia, jossa tiedostoja muokataan lokaalisti ja synkronoidaan etävaraston kanssa.

Tämä vastaa tehtävänannon vaatimusta, jossa repository kloonataan, siihen tehdään muutoksia ja varmistetaan, että ne näkyvät GitHubissa.

<img width="1281" height="216" alt="image" src="https://github.com/user-attachments/assets/6db614b9-7d18-49d0-b8ab-41ef7199cf15" /> **Kuvassa uusi tiedosto b_dolly.txt luotuna projektikansioon.**

Loin projektikansioon uuden tiedoston nimeltä b_dolly.txt ja lisäsin siihen sisältöä. Tämän jälkeen tarkistin tiedoston olemassaolon sekä sisällön ls- ja cat-komennoilla.

<img width="1279" height="310" alt="image" src="https://github.com/user-attachments/assets/4c9a1f5b-19d3-425d-b5da-b4c7dae8ec31" /> **Kuvassa muutokset lähetetään GitHubiin git push -komennolla.**

Seuraavaksi lisäsin tiedoston versionhallintaan git add --all -komennolla. Tämän jälkeen tallensin muutokset git commit -komennolla, jossa annoin kuvauksen tehdyistä muutoksista.

Lopuksi lähetin muutokset GitHubiin git push -komennolla, jolloin paikalliset muutokset siirtyivät etävarastoon.

<img width="946" height="405" alt="image" src="https://github.com/user-attachments/assets/09d0974f-691c-400d-b6b1-b768c650ca55" /> **Kuvassa GitHub-repository, jossa uusi tiedosto b_dolly.txt näkyy onnistuneesti git push -komennon jälkeen.**

Lopuksi tarkistin GitHubista, että uusi tiedosto oli ilmestynyt repositoryyn onnistuneesti. Kuvasta näkyy, että b_dolly.txt-tiedosto on lisätty ja commit-viesti näkyy oikein.

Tämä vahvistaa, että muutokset siirtyivät onnistuneesti paikalliselta koneelta GitHubiin ja versionhallinta toimii odotetulla tavalla.

### c.) Doh!

Tässä tehtävässä kokeiltiin tilanteen peruuttamista Gitissä ilman commitointia. Tavoitteena oli tehdä muutos tiedostoon, lisätä se staging-alueelle ja tämän jälkeen peruuttaa kaikki muutokset git reset --hard -komennolla.

Tämä vastaa tehtävänannon vaatimusta, jossa huonot muutokset poistetaan ennen commitointia.

<img width="1279" height="235" alt="image" src="https://github.com/user-attachments/assets/2f9eaab0-4e08-4dd5-b9b6-10b4115d5c6f" /> **Kuvassa tiedostoon b_dolly.txt tehty muutos ennen versionhallintaan lisäämistä.**

Muokkasin olemassa olevaa tiedostoa b_dolly.txt ja lisäsin siihen uuden rivin. Tämä simuloi tilannetta, jossa tiedostoon tehdään virheellinen tai ei-toivottu muutos.

<img width="1281" height="301" alt="image" src="https://github.com/user-attachments/assets/231123df-0fc5-4375-bbbc-fe973e465acb" /> **Kuvassa muutos lisättynä staging-alueelle git add -komennolla, mutta sitä ei ole vielä commitoitu.**

Seuraavaksi lisäsin muutoksen staging-alueelle git add --all -komennolla. Tässä vaiheessa muutos on valmis commitointia varten, mutta sitä ei ole vielä tallennettu versionhallinnan historiaan.

<img width="1281" height="187" alt="image" src="https://github.com/user-attachments/assets/067a4cf5-e281-4bd3-b797-bec5e4aae4fb" /> **Kuvassa git reset --hard -komennon jälkeen ei ole muutoksia, eli kaikki tekemättömät muutokset on poistettu.**

Tämän jälkeen peruutin kaikki tekemättömät muutokset git reset --hard -komennolla. Tämä komento palauttaa projektin viimeisimpään commit-tilaan ja poistaa kaikki staging-alueella ja työtilassa olevat muutokset.

<img width="1285" height="174" alt="image" src="https://github.com/user-attachments/assets/50937ef8-950e-46f7-a828-8cd6d6197a72" /> **Kuvassa tiedoston sisältö resetoinnin jälkeen. Aiemmin tehty muutos on positunut.**

Lopuksi tarkistin tiedoston sisällön ja huomasin, että aiemmin tehty muutos oli poistunut. Tämä vahvistaa, että git reset --hard toimii tehokkaana tapana peruuttaa ei-toivotut muutokset ennen commitointia.

On tärkeää huomata, että tämä komento poistaa muutokset pysyvästi, eikä niitä voi palauttaa helposti.

### d.) Tukki

